# License

## 内核（本仓库默认条款）

UNLICENSED — 保留所有权利。

本仓库主体（`bin/` `src/` `test/` `ui/` `config.json` 等）为**闭源内核**：
未经书面许可，不得复制、修改、分发、再许可或用于商业目的。

内核运行于「冻结 APK 容器」的 Node 运行时之上，作为控制面板 / 生命周期管理器（Manager）；
其构建物随安卓容器 APK 经 OTA 分发，构建物授权范围同样以本许可为准（UNLICENSED，全部权利保留）。

> 内核不自托管发布 / 下载逻辑（单写入者契约）：无 launcher、无 SEA 单文件、无 npm 平台子包发布；
> 面板（UI）经 `ui/dist` 同源托管，或随 OTA 注入 APK。
