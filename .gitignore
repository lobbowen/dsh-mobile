# 构建产物
.gradle/
container/app/build/
build/
local.properties
*.iml
.idea/

# 编译出的 Node 二进制（体积大，按需本地生成，不入库）
container/app/src/main/assets/node-bin/

# 内核**不再随 APK 分发**（ADR-0005）：APK 内不允许出现内核资产。
#
# 所以这里**不再忽略任何内核包** —— 目的是让"有人把内核塞回 assets/kernel/"
# 这件事**可见**：CI 的 APK 审计会因为"发现内核资产"直接报错，而不是被 ignore 静默吞掉。
# （历史上这个文件曾让一个 1.2MB 的 baseline.zip 签进过仓库，正是 ignore 掩盖的结果。）

# 内嵌 npm 资产（scripts/stage-npm-assets.sh 从 registry 拉取官方 tarball 生成；
# 版本+sha512 双钉在脚本里，可重建、不必入库）
container/app/src/main/assets/npm/

# OTA 发布包
release/

# 内核 feed 目录（由 scripts/build-kernel-feed.sh 产出，是**投递产物**而非源码）
#
# 与服务端 feed 同理：它内容是「内核仓某 commit + 某个私钥」的函数，
# 入库会立刻脱节，且每次构建字节都不同 → 无意义的 diff。
# 它存在的意义是「投给设备」，而投递本身走 Release / adb push，不需要经过 git。
feed/

# 沙箱产物
*.log
.DS_Store

# ============================================================================
#  密钥目录：**白名单制**（默认全忽略，只放行明确安全的文件）
# ============================================================================
#  为什么改成白名单（真实踩过的坑）：
#  原先这里是按后缀拉黑的 —— `keys/ota-private.pem` + `keys/*.pem`。
#  本轮新增 APK 签名 keystore（keys/release.keystore）后，它**不在**这两条
#  规则里，于是 `git add -A` 直接把签名私钥和它的密码文件（keystore.properties）
#  暂存了。本地 `git status` 里它们和普通改动长得一模一样，极易顺手提交。
#
#  按后缀拉黑的根本问题：每引入一种新密钥形态，都必须记得回来补一条规则，
#  漏一次就是一次不可逆的私钥泄漏（推到远端就等于换 key 身份，Android 生态
#  没有"换回旧签名"的机制）。白名单反过来 —— 默认拒绝，只放行显式安全的，
#  漏配的代价只是"某个文件没被跟踪"，而不是"私钥被公开"。
#
#  放行的只有 README（纯文档）。其余一律忽略，包括：
#    · ota-private.pem     内核 ed25519 私钥
#    · release.keystore    APK 签名 keystore
#    · keystore.properties 上述两者的密码
# ============================================================================
keys/*
!keys/README.md

# ── 凭据纵深防御（即使误放进仓库也不入库）──
.secrets/
*.token
*.keystore
keystore.properties
.env
.env.*
